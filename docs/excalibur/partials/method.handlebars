<a name="methods_{{name}}"></a>
<div class="method item{{#if access}} {{access}}{{/if}}">
    {{#if final}}<code>final</code> {{/if}}{{#if returnType}}{{#crossLink returnType}}{{/crossLink}} {{/if}}<pre>{{#if is_constructor}}new {{/if}}{{name}}({{paramsList}}){{#if return}}{{#return}}: {{#strip description}}{{/strip}}{{/return}}{{#if static}} <em>(static)</em>{{/if}}{{/if}} {{#if access}}<em>`/* {{access}} method */`</em>{{/if}}</pre>
    <hr>
    <span class="foundat">
    {{#if overwritten_from}}
        Defined in <a href="{{overwritten_from/class}}.html#method_{{overwritten_from/name}}">{{overwritten_from/class}}</a> but overwritten locally: 
    {{else}}
        {{#if extended_from}} Defined in <a href="{{extended_from}}.html#method_{{name}}">{{extended_from}}</a>: {{/if}}
    {{/if}}
        
    </span>
    {{{methodDescription}}}
    {{#if params}}
    <strong class="parameters">Parameters:</strong>
    <table class="props">
      <thead>
         <tr>
            <th>Name</th>
            <th>Type</th>
            <th>Description</th>
         </tr>
      </thead>
      <tbody>

    {{#params}}
        <tr>
        {{#if optional}}
            <td>`[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]`</td> <td><strong>{{#crossLink type}}{{/crossLink}}</strong></td><td>{{{description}}}</td>
        {{else}}
            <td>`{{name}}`</td> <td><strong>{{#crossLink type}}{{/crossLink}}</strong></td><td>{{{description}}}</td>
        {{/if}}
        {{#if multiple}}
            <abbr title="Denotes that this argument can contain (n) number">(*..n)</abbr>
        {{/if}}
            {{#if props}}
            <ul>
            {{#props}}
                <li>`{{name}}` <strong>&lt;{{#crossLink type}}{{/crossLink}}&gt;</strong> {{{description}}}
                {{#if props}}
                    <ul>
                        {{#props}}
                            <li>`{{name}}` <strong>&lt;{{#crossLink type}}{{/crossLink}}&gt;</strong> {{{description}}} </li>
                        {{/props}}
                    </ul>
                {{/if}}
                </li>
            {{/props}}
            </ul>
            {{/if}}
        </tr>
    {{/params}}
      </tbody>
    </table>
    <br/>
    {{/if}}
    {{#if example}}
        <h5>Example</h5>
        {{{example}}}
    {{/if}}
</div>
